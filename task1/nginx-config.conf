server {
    listen 80;
    server_name localhost;

    location /files {
        alias /home/denis/DZ3/task1/files;
        autoindex on;
        autoindex_exact_size off;
        autoindex_localtime on;
    }

    location / {
        root /var/www/html;
        index index.html;
    }
}