# Задание 1: Раздача статических файлов через Nginx

## Цель
Настроить Nginx таким образом, чтобы при обращении к URL `/files` отображался список файлов (directory listing), аналогично [зеркалу Debian](https://mirror.yandex.ru/debian/tools/).